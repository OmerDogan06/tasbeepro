<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    
    <!-- Widget Config Activity Theme -->
    <style name="WidgetConfigTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#F8F6F0</item>
        <item name="android:colorPrimary">#2D5016</item>
        <item name="android:colorPrimaryDark">#1A3409</item>
        <item name="android:colorAccent">#D4AF37</item>
        <item name="android:textColorPrimary">#2D5016</item>
        <item name="android:textColorSecondary">#5D6B3D</item>
        
        <!-- Switch için Islamic renkler -->
        <item name="android:colorControlActivated">#D4AF37</item>
        <item name="android:colorControlHighlight">#F5E6A8</item>
        
        <!-- Spinner için renkler -->
        <item name="android:spinnerDropDownItemStyle">@style/SpinnerDropDownItemStyle</item>
    </style>
    
    <!-- Spinner dropdown item style -->
    <style name="SpinnerDropDownItemStyle" parent="@android:style/Widget.DropDownItem.Spinner">
        <item name="android:textColor">#2D5016</item>
        <item name="android:background">#FFFDF7</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>
</resources>
